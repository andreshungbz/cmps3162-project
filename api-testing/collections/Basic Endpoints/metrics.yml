info:
  name: metrics
  type: http
  seq: 2

http:
  method: GET
  url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/observability/metrics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # metrics

  Displays information from the `expvar` Go package.
