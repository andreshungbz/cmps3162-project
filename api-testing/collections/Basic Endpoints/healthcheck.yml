info:
  name: healthcheck
  type: http
  seq: 1

http:
  method: GET
  url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/healthcheck"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # healthcheck

  Endpoint for checking the API server status and system information.
