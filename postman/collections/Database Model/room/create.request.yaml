$kind: http-request
name: create
description: Creates a room record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/hotels/1/rooms"
method: POST
body:
  type: json
  content: |-
    {
      "number": 501,
      "room_type_id": 1,
      "floor": 5,
      "status_code": "V/C"
    }
auth:
  type: bearer
  credentials:
    token: "{{TEST_OM_AUTH_TOKEN}}"
order: 3565879233170171
