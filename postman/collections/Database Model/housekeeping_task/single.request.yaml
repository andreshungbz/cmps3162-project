$kind: http-request
name: single
description: Retrieves a single housekeeping_task record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/housekeeping_tasks/1"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{TEST_HK_AUTH_TOKEN}}"
order: 1773552681936
