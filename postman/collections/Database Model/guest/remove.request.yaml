$kind: http-request
name: remove
description: Removes a guest record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/guests/A1234567"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{TEST_FD_AUTH_TOKEN}}"
order: 4490076469354738
