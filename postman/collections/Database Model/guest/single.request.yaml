$kind: http-request
name: single
description: Retrieves a single guest record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/guests/A1234567"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{TEST_FD_AUTH_TOKEN}}"
order: 1773594017676
