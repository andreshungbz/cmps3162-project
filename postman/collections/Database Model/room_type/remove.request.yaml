$kind: http-request
name: remove
description: Removes a room_type record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/room_types/2"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{TEST_OM_AUTH_TOKEN}}"
order: 3414287422082933
