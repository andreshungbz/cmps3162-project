$kind: http-request
name: remove
description: Removes a department record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/departments/Guest
  Services & Front Desk"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{TEST_OM_AUTH_TOKEN}}"
order: 4094924049524000
