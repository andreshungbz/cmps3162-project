$kind: http-request
name: create
description: Creates a registration record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/registrations"
method: POST
body:
  type: json
  content: |-
    {
      "reservation_id": 2,
      "hotel_id": 1,
      "room_number": 401
    }
auth:
  type: noauth
order: 4477159140892943
