$kind: http-request
name: housekeeper-by-id
description: Retrieves a houskeeper employee by their ID.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/housekeepers/3"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{TEST_OM_AUTH_TOKEN}}"
order: 4494061855367413
