$kind: http-request
name: single
description: Retrieves a single reservation record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/reservations/2"
method: GET
auth:
  type: noauth
order: 1773599795582
