$kind: http-request
name: remove
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/reservations/2"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{TEST_FD_AUTH_TOKEN}}"
order: 4502984861775993
