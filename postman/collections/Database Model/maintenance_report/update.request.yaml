$kind: http-request
name: update
description: Updates a maintenance_report record.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/maintenance_reports/4"
method: PUT
body:
  type: json
  content: |-
    {
      "description": "Light fixture wiring is bad",
      "completed": true
    }
auth:
  type: bearer
  credentials:
    token: "{{TEST_HK_AUTH_TOKEN}}"
order: 4287968450269971
