$kind: http-request
name: remove
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/maintenance_reports/4"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{TEST_HK_AUTH_TOKEN}}"
order: 4352704857453407
