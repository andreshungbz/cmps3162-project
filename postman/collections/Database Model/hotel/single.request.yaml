$kind: http-request
name: single
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/hotels/1"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{TEST_OM_AUTH_TOKEN}}"
order: 1773546834756
