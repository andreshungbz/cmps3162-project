$kind: http-request
name: healthcheck
description: Checks the server status and system information.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/healthcheck"
method: GET
auth:
  type: noauth
order: 1773500463541
