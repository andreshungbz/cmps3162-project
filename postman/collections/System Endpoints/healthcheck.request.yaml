$kind: http-request
name: healthcheck
description: Checks the API server status and system information.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/healthcheck"
method: GET
order: 1773500463541
