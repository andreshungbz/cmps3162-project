$kind: http-request
name: new-token
description: Creates an authentication token for an account.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/tokens/authentication"
method: POST
body:
  type: json
  content: |-
    {
        "work_email": "dario@grandoceanview.com",
        "password": "hotel_password"
    }
examples: .resources/new-token.resources/examples
order: 1773523060971
