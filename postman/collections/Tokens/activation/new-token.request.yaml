$kind: http-request
description: Creates an activation token for an account.
url: "{{SERVER_ADDRESS}}:{{SERVER_PORT}}/{{API_VERSION}}/tokens/activation"
method: POST
body:
  type: json
  content: |-
    {
        "work_email": "bea@grandoceanview.com"
    }
order: 1773522999565
